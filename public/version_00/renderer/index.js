import browserRendererConfig from '../config/browserRendererConfig.js';
import createRenderer from './createRenderer.js';

const renderer = createRenderer(browserRendererConfig);
export default renderer;
